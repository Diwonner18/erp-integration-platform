

# Alterar "Tipo de Serviço" para Input de Texto

## Mudança

No `SolicitarAgendamento.tsx`, trocar o `<select>` de "Tipo de Serviço" por um `<Input>` de texto livre, permitindo que o cliente descreva o serviço desejado. Isso segue o padrão já adotado no sistema de entrada manual.

## Arquivo Modificado

| Arquivo | Mudança |
|---|---|
| `SolicitarAgendamento.tsx` | Substituir `<select>` por `<Input placeholder="Descreva o tipo de serviço desejado">` |

